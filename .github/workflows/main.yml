name: Keep Supabase Alive

on:
  workflow_dispatch: 
  schedule:
    # Every 6 days at midnight UTC
    - cron: "0 0 */6 * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Call API
        run: |
          curl https://http://notes-organizer-aba.vercel.app//api/health
